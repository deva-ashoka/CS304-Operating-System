(Terminal) Commands to execute the files:

1. To create the sort1 executable file: gcc -o sort1 sort1.c
2. Compile xsort.c: gcc xsort.c
3. Execute (replace “input.txt” with the file name): ./a.out input.txt