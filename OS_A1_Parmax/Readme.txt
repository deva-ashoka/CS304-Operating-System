(Terminal) Commands to execute the files:

1. To create the parmax executable file: gcc -o parmax parmax.c
2. Compile wrapper.c: gcc wrapper.c
3. Execute (replace #number with the number of integers): ./a.out #number